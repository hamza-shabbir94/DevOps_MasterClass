FROM ubuntu:latest

LABEL version="0.0.1"
LABEL maintainer="Hamza Shabbir"

RUN apt-get update && apt-get upgrade -y

RUN apt-get install nginx -y

EXPOSE 80

CMD [ "nginx", "-g", "daemon off;" ]

# docker build -t hamza94/nginx_custom:0.0.1 .